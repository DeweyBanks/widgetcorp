<p id="notice"><%= notice %></p>
<div class="row-fluid">
  <div class="col-md-8-offset-2">
    <div id="instructions" class="page-header bg-info">
      <h2>Widgets</h2>
      <small>See something you like? Start a negotiation!</small>
    </div>
    <div class="row-fluid">
      <% @widgets.each do |widget| %>
        <div class="col-sm-2 col-md-4">
          <div class="thumbnail right-caption span4">
            <%= image_tag widget.image.url(:medium), :class => "wiget-image pull-left" %>
            <div class="caption text-center">
              <h3><%= widget.title %></h3>
              <p>seller: <%= widget.user.username %></p>
              <p>asking price: <%= number_to_currency(widget.price) %></p>
              <p class="discription"><%= widget.description %></p>
              <p><a href="#" class="btn btn-primary" data-toggle="modal" data-target="#notifyModal" role="button">I want this!</a></p>
            </div>
          </div>
        </div>

        <!-- -Modal- -->
        <div class="modal fade" id="notifyModal" tabindex="-1" role="dialog" aria-labelledby="notifyModalLabel">
          <div class="modal-dialog" role="document">
            <div class="modal-content">
              <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                <h4 class="modal-title" id="myModalLabel">Notify intent to purchase</h4>
              </div>
              <div class="modal-body">
                <div>
                  <h3><%= widget.title %></h3>
                  <p>seller: <%= widget.user.username %></p>
                  <p>asking price: <%= number_to_currency(widget.price) %></p>
                </div>
              </div>
              <div class="modal-footer">
                <div class="text-justify">
                  <p>Here you can notify the seller that you would like to purchase this item.</p>
                  <p>Enter an optional note to the seller and the price you're willing to pay</p>
                </div>
                <form class="form-horizontal" action="">
                  <div class="form-group">
                    <div class="col-sm-10">
                      <input type="text" class="form-control" placeholder="Note to seller">
                    </div>
                  </div>
                  <div class="form-group">
                    <div class="col-sm-10">
                      <input type="number" class="form-control" placeholder="Price">
                    </div>
                  </div>
                  <div class="form-group">
                    <div class="col-sm-offset-2 col-sm-10">
                      <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
                      <button type="button" class="btn btn-primary">Submit</button>
                    </div>
                  </div>
                </form>
              </div>
            </div>
          </div>
        </div>

        <!-- -End Modal -->
      <% end %>
    </div>
  </div>
</div>





